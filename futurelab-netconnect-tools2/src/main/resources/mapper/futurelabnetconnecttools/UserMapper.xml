<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.net.tools.futurelabnetconnecttools.service.provider.dal.mapper.UserMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.net.tools.futurelabnetconnecttools.service.provider.dal.entity.BaseUser">
        <result column="id" property="id" />
        <result column="login_name" property="loginName" />
        <result column="password" property="password" />
        <result column="phone" property="phone" />
        <result column="is_deleted" property="isDeleted" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="columns">
        id,
        login_name,
        password,
        phone,
        id_deleted
    </sql>

    <!-- 通用查询表名 -->
    <sql id="tableName">
        base_user
    </sql>

</mapper>
